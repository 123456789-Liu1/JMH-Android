# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 规范。

## [1.0.1] - 2026-09-25

### 变更

- **统一发布签名**：改用固定密钥签名，确保此后所有版本都能直接覆盖安装并保留用户数据与设置
- 修复顶部按钮上半部分被状态栏遮挡、在部分机型上点击无响应的问题
- CI 增加签名校验步骤，构建产物签名可追溯

### ⚠️ 升级须知

v1.0.0 使用的是调试签名，与本版本签名不同，**无法直接覆盖安装**。
从 v1.0.0 升级需先卸载旧版（应用内数据会被清除，请先解密导出重要文件）。

**从本版本开始，后续升级均可直接覆盖安装，数据与设置完整保留。**

## [1.0.0] - 2026-09-24

### 首个版本

**核心功能**

- 首次启动强制设置访问密码（≥6 位，支持数字与字母组合）
- 文件加密：AES-256-GCM + PBKDF2-HMAC-SHA256（150,000 次迭代）
- 自定义 `.jmh` 加密容器格式，含原始文件名 / MIME / 大小等元信息
- 主密钥分层设计：修改密码无需重新加密任何文件
- 支持一次选择并加密多个文件

**浏览与预览**

- 解密预览仅写入应用私有缓存，退出即清理，不落盘到用户可见存储
- 图片、文本内置预览；其余格式可调起系统应用打开
- 支持将加密文件完全解密导出到任意位置

**媒体播放**

- 内置视频 / 音频播放器
- 倍速播放（0.5x / 1x / 1.25x / 1.5x / 2x / 3x）
- **长按画面临时 3 倍速，松手自动恢复**

**存储与设置**

- 双存储位置：应用专属目录（默认，零权限）/ 用户授权的公共目录（SAF）
- 首次使用引导选择位置，可跳过
- 切换位置时自动迁移已有加密文件（先复制校验、再删除源文件）
- 修改密码需验证原密码并对新密码二次确认

**工程**

- Kotlin + Jetpack Compose + Material 3
- 支持 Android 10（API 29）及以上
- 9 项设备端自动化测试覆盖加密引擎与业务链路
- 完全离线，未声明任何网络权限

# 安全策略

## 设计原则

JMH 是一个**纯本地**的加密工具，安全模型建立在以下前提上：

1. **零联网**：应用未声明 `INTERNET` 权限，不存在任何数据上传、遥测或云端同步。
2. **算法公开、密钥保密**（Kerckhoffs 原则）：加密算法完全公开，安全性仅依赖用户密码。
3. **无后门**：不存在任何形式的密码找回、万能密钥或管理入口。
4. **最小权限**：仅访问用户主动选择或授权的文件。

## 加密实现

| 环节 | 方案 |
|------|------|
| 内容加密 | AES-256-GCM（认证加密，防篡改） |
| 密钥派生 | PBKDF2-HMAC-SHA256，150,000 次迭代，随机 16 字节盐 |
| 密钥分层 | 主密钥随机生成，由密码派生的 KEK 加密保存 |
| 每文件密钥 | 每个文件独立随机 DEK，被主密钥包装后存入文件头 |
| 完整性保护 | 整个文件头作为内容加密的 AAD，任何篡改都会导致解密失败 |

### 为什么修改密码不需要重新加密文件？

因为文件内容由**独立的数据密钥（DEK）**加密，而 DEK 由**主密钥**包装。
修改密码时只需用新密码重新包装主密钥，所有历史文件即刻生效。

### 已知的安全边界

- **密码强度决定一切**：纯数字短密码容易被离线暴力破解。建议使用 6 位以上、包含字母的组合。
- **密码遗忘即数据永久丢失**：这是设计使然，请务必牢记密码或妥善备份。
- **应用专属目录下，卸载应用会删除加密文件**：请在卸载前导出。
- **未获得 root 权限的设备上**：应用私有缓存中的解密预览文件在退出预览时会被清理，
  但已被系统快照/备份机制捕获的内容不在本应用控制范围内。

## 报告安全问题

如果你发现安全漏洞，**请不要直接提交公开 Issue**。请通过以下方式私下联系：

- 在 GitHub 上通过 [Security Advisories](../../security/advisories/new) 提交私密报告

我们会在确认后尽快修复，并在修复版本中致谢（如你希望署名）。

### 报告时请包含

- 问题描述与影响范围
- 复现步骤或 PoC
- 受影响的版本
- 你建议的修复思路（可选）

## 不在范围内

- 因用户遗忘密码导致的数据丢失
- 因设备已被 root / 植入恶意软件导致的信息泄露
- 用户将密码明文记录在便签等不安全位置
